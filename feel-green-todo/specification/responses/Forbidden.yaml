description: 403 Forbidden - the server understood the request but refuses to authorize it
content:
  application/json:
    schema:
      type: object
      required:
        - message
      properties:
        message:
          type: string
          description: Message explaining reason of error
          example: 403 Forbidden - You do not have permission to access the resource on this server
